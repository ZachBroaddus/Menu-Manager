$('.card-footer').on('click', '.js--add-to-order', function(){
  // console.log('add button clicked!');
  var $target = $(this);
});

$target.closest('.card-footer').html("<%= escape_javascript(render partial: 'order_items/card_footer', locals: { item: @item, order_item: @order_item }) %>");
